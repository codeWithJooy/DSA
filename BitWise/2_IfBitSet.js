let num = 5; //number to check.Represented as 101
let k = 1; // Check if Kth bit is set.Indexing starts from 0 from LSB

console.log(`Check of ${k}th bit is set in ${num} using left Shift is`);

console.log(`Check of ${k}th bit is set in ${num} using Right Shift is`);
